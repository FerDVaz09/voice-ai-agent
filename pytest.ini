[pytest]
# Agrega la raíz del proyecto al sys.path para que 'api' y 'tools' sean importables en CI
pythonpath = .
testpaths = tests
# pytest-asyncio: auto detecta tests async sin necesidad de @pytest.mark.asyncio
asyncio_mode = auto
